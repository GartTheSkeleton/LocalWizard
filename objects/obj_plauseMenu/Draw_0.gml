if (visible) {
    // Draw the menu
    draw_set_color(c_white);
    draw_text(room_width / 2, room_height / 2, "Level Up Menu");
    
    // Add code to draw menu options, handle player input, etc.
}
